import 'package:flutter/material.dart';
class RegistrarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Registrar\'s Office')),
      body: Center(child: Text('Welcome to Registrar\'s Office')),
    );
  }
}