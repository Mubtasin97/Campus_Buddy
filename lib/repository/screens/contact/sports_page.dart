import 'package:flutter/material.dart';
class OsaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Office of Student Affairs (OSA)')),
      body: Center(child: Text('Welcome to OSA')),
    );
  }
}