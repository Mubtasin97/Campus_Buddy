import 'package:flutter/material.dart';
class SportsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Office of Sports (OS)')),
      body: Center(child: Text('Welcome to Sports Office')),
    );
  }
}