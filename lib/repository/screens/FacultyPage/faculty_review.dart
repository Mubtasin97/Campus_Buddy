import 'package:flutter/material.dart';

class FacultyReview extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Review")),
      body: Center(child: Text("This page will show reviews")),
    );
  }
}
